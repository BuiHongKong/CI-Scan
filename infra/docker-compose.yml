services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "3000:3000"
      - "9229:9229"
    privileged: true
    environment:
      APP_NAME: CI-Scan
      DATABASE_PASSWORD: demo-password-in-compose
    security_opt: []
    read_only: false
