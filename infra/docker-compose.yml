services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "3000:3000"
    environment:
      APP_NAME: CI-Scan
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
